version: "2"
authtoken: <set in production environment>

tunnels:
  website:
    addr: 80
    basic_auth: 
      - "<set uid in prod environment>:<set pwd in prod environment>"
    schemes:
      - https
    inspect: false
    proto: http
    domain: tequacreek.ngrok.io
